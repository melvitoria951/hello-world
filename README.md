# hello-world
Praticando Fluxo no github

#include <stdio.h>

int main ()  {
  int idade = 30;
  float peso = 65.5;
  char inicial = 's'
 

  printf("idade: %d\\n", idade);
  printf("peso: %.1f\\n", peso);
  printf("inicial: %c\\n", inicial);

  return 0;
 }
